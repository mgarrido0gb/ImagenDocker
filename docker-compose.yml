version: '3'

services:
  app: 
    build: 
      context: ./
    
    ports:
      - "4000:4000"
   
   #si se realiza un cambio se vera reflejado
    volumes: 
      - .:/app
    environment:
       FLASK_DEBUG: development
  redis:
    image: "redis:alpine"